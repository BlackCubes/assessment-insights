repos:
  - repo: https://github.com/ddanier/sub-pre-commit.git
    rev: v4.5.1
    hooks:
      - id: sub-pre-commit
        alias: backend
        name: "pre-commit for backend/"
        args: ["-p", "backend"]
        files: "^backend/.*"
        stages: ["commit"]
